using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class ChatScript : MonoBehaviour
{

    /*Transform textBox;
    Item Item;
    collect collect;
    public GameObject item;
    public bool Tagged = false;
    public bool Returned = false;
    public int coin;
    GameManager GameManager;

    void Start()
    {
        Item = item.GetComponent<Item>();
        textBox = transform.GetChild(0);
        collect = GameObject.Find("Collectibles").GetComponent<collect>();
        GameManager = GameObject.Find("GameManager").GetComponent<GameManager>();
    }


    void Update()
    {

    }

    private void OnTriggerEnter2D(Collider2D col)
    {
        print("Chat");
        textBox.gameObject.SetActive(true);
        Tagged = true;
        if (!gameObject.activeInHierarchy && !Returned || Item.Collected && !Returned)
        {
            GameManager.coins += coin;
        }
        if (!gameObject.activeInHierarchy || Item.Collected)
        {
            textBox.GetChild(1).gameObject.SetActive(true);
            textBox.GetChild(0).gameObject.SetActive(false);
            Returned = true;
        }

        
        
    }

    private void OnTriggerExit2D(Collider2D collision)
    {
        print("Chat");
        textBox.gameObject.SetActive(false);
        
    }*/
}